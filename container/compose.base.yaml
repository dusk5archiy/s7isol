# compose.base.yaml
services:
  container-base:
    build:
      context: .
      dockerfile: Dockerfile
      target: base
    image: saoyui/s7container-base:latest
    container_name: s7container-base
    working_dir: /home/adevuser/workspace
    restart: unless-stopped
